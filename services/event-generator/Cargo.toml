[package]
name = "event-generator"
version = "0.1.0"
edition = "2021"
description = "Generates timeline events for testing and cloud deployments"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
